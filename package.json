{
  "name": "conversation-span-viewer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "bun run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "gh-pages": "^6.1.1",
    "typescript": "^5.5.3",
    "vite": "^5.4.8"
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
